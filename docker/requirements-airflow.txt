# Runtime dependencies the Airflow workers need to execute DataForge tasks.
# (dbt lives in its own virtualenv - see Dockerfile.airflow.)
pandas>=2.2
numpy>=1.26
pyarrow>=15
pyspark>=3.5
psycopg[binary]>=3.1
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.27
python-dotenv>=1.0
