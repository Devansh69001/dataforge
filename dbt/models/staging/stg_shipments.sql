select
    shipment_key,
    shipment_id,
    order_key,
    order_id,
    customer_key,
    warehouse_key,
    warehouse_id,
    region_key,
    region_code,
    carrier,
    order_date,
    order_month,
    label_created_ts,
    delivered_ts,
    exception_ts,
    returned_ts,
    delivery_hours,
    delivery_days,
    is_delivered,
    had_exception,
    is_returned,
    timeline_inconsistent
from {{ source('warehouse', 'fact_shipping') }}
